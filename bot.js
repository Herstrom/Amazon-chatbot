// Wait for the DOM to be fully loaded
document.addEventListener('DOMContentLoaded', function () {
    // Get the button element by its ID
    const sendButton = document.getElementById('sendButton');
  
    // Add an event listener to the button
    sendButton.addEventListener('click', function () {
      // This is where you can add your logic when the button is clicked
      console.log("Button clicked! Let's send something.");
      // You could trigger a form submission, send an email, or any other action here
      // For example, simulate sending a message:
      
    });
  });
 
  
  